{
  "name": "letjunior-wa-agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "vercel dev",
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "PACING_ENABLED=false node --import tsx --test tests/*.test.ts",
    "seed": "tsx scripts/seed.ts"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.4",
    "@vercel/functions": "^1.5.0"
  },
  "devDependencies": {
    "@types/node": "^22.7.5",
    "@vercel/node": "^3.2.24",
    "tsx": "^4.19.1",
    "typescript": "^5.6.3"
  }
}
